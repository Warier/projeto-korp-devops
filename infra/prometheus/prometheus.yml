global:
  scrape_interval: 15s
  scrape_timeout: 5s

scrape_configs:
  - job_name: projeto-korp
    metrics_path: /metrics
    static_configs:
      - targets:
          - http-server-projeto-korp:8080
