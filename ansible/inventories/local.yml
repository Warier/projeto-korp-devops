all:
  children:
    korp:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
          ansible_become: false
          install_docker: false
          deployment_path: "{{ playbook_dir | dirname }}"
